# Remote Hosts 0.4.0：已发布并完成三端验收

2026-09-11。项目位于 `/Users/jinliang/Workspace/remote_hosts`。NAS网关、MacBook-M2-Max、Mac-Studio均已升级到0.4.0；两台Mac的常规和新增工作流现场验收通过。Studio已按用户最新授权纳入本版，不再沿用旧的延期范围。

## 实际交付

| 目标 | 运行结果 | 原始证据 |
|---|---|---|
| NAS网关 | 0.4.0，PID28645，运行哈希匹配 | `nas-updater-q1.json` |
| MacBook | 0.4.0，PID8130，11次采样、约19.9秒稳定，五通道持续前进 | `macbook-updater-q1.json` |
| Mac Studio | 0.4.0，PID15386，10次采样、约24.5秒稳定，五通道持续前进 | `studio-updater-q1.json`、`studio-runtime-q1.json` |

两台Mac的一次性更新器分别仅运行1次、退出0。原二进制和回退备份保留，没有重复安装已成功目标，没有改写历史发布包，没有提交或推送Git。

正式发布包：`dist/remote-hosts-code-0.4.0/`。

- manifest SHA-256：`2733d348dbef6f1293304c5805d9dba348cc3422c6fbdece0d956a6a496f24d1`。
- macOS ARM64：`73e0bd2de3746cc5044f73a1107af9791f7e7f28d005091298cbcbef68c6faee`。
- Linux AMD64：`18c80eec5b318c51bd678cb6e21bf3b2d0ffad1c242b9e585b816b82761df718`。

## 本版能力

双向文件传输使用4 MiB持久检查点、原任务身份和接收端确认偏移；进程重启恢复不重放任意终端命令。新增 `transfer_resume` 支持同任务恢复和新的来源授权，新增 `transfer_cancel` 区分取消请求与实际清理完成，新增 `workspace_context` 返回所选工作区的传输、终端和运行事实。服务端目录共18项工具。

恢复数据绑定设备、工作区、来源身份和原始版本条件；迟到旧代回执不能覆盖新尝试。进度区分已写入与持久确认字节，恢复的旧前缀不计为新吞吐。构建槽异常持有者未确认清理时，不直接重用其目录。

## 验证

固定快照 `29631f9b0328e8e0efec205029d90cbc9decac9fd940758f33557635658adfa7`，133个输入。205项Rust、85项Python，共290项通过、0失败。格式、严格Clippy和全工作区检查通过。另1项需主动启用的Python探针及2项性能用例未计入通过数。

两平台发布构建和包哈希检查通过，输入没有漂移。验证阶段386.8秒，macOS构建145.9秒，Linux构建158.6秒；这不是网络或模型响应速度基准。

| 现场门禁 | 结果 |
|---|---|
| 两台Mac代码读取、搜索、符号、精确修改、幂等与版本冲突 | 通过 |
| 两台Mac终端执行 | 通过 |
| 两台Mac各2 MiB二进制上传下载、HTTP Range、重试、禁止覆盖与错误SHA拒绝 | 通过 |
| 两台Mac来源授权失效后，以原operation_id恢复 | 通过 |
| 两台Mac恢复请求幂等、不重复执行 | 通过 |
| 两台Mac暂停中的任务取消及清理确认，目标未被写入 | 通过 |
| 两台Mac工作区事实快照及游标 | 通过 |

常规证据为 `acceptance-two-devices-q1.json`，新增工作流证据为 `workflow-acceptance-q1.json`。临时OAuth授权已撤销，工作流源文件已核对后删除。新增接口通过正常授权的MCP路径验证，不冒充当前聊天宿主已经刷新旧工具声明。

## 本次收尾修复与保留记录

先修复上一轮三处格式差异，重新冻结并完整复验，没有继承失败快照的通过结论。发布入口仅接受完成的构建记录，缺失记录立即停止，而不是空等一小时。

实际升级过程中，发布观察器比Studio稳定回执落盘更早读取，因而退出。随后读取原更新任务，核对两台运行哈希与成功回执，继续完成全部验收，没有重装或重启任何已完成目标。原失败记录保存在 `deployment-q1-before-receipt-recovery.json`；通用发布器中的有界回执等待和阶段恢复仍按RH-047继续优化。

两项附加操作被宿主在执行前拒绝：单独复制验证日志、另建原生附件补充检查脚本。它们没有执行，也没有改包装重放。既定发布流程和两台实际MCP功能验收正常完成；不将未执行的补充检查列为通过。

## 明确保留的边界

单文件仍为64 MiB，检查点保留24小时。来源恢复需要强验证器或原始预期SHA；授权过期后显式补充授权。已提交的发布不能被迟到取消撤回。工作区上下文不是完整Git验收记录或聊天历史。宿主工具声明刷新、通用发布恢复及更广泛故障矩阵仍在产品清单中，不因版本上线而自动关单。

主发布事实：`deployment.json`。源码验证：`verification-q1.json`。准确源码归档：`verified-source-q1.tar.gz`。检查日志：`logs-q1/`。问题清单：`docs/product/backlog.json`。
